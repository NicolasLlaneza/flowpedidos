# Secrets and local config
.env
.env.local
*.key
*.pem

# Docker volumes (gestionados por Docker, no por Git)
volumes/

# n8n local data si se usa bind mount
.n8n/

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Datos sensibles del experimento
dataset/real/
dataset/*.private.*

# Python (por si después agregamos scripts)
__pycache__/
*.pyc
.venv/
venv/

# Node (por si agregamos scripts JS)
node_modules/

# Demo CLI
demo/node_modules/

# Fixtures del dataset copiados al mock por reset.mjs (runtime)
mocks/data/orders/30*.json

# Material de presentación/tesis (no es parte del funcionamiento de la app)
docs/
video/

# Config de herramientas de dev (no es parte de la app)
.claude/
