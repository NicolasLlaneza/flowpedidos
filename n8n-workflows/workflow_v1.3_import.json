{
  "name": "My workflow",
  "nodes": [
    {
      "id": "3cecebb3-af50-40a9-b13c-93bafb56816f",
      "name": "Webhook ML order",
      "type": "n8n-nodes-base.webhook",
      "position": [
        0,
        0
      ],
      "webhookId": "2f39f422-d721-4e45-8180-ec600db1ee76",
      "parameters": {
        "path": "ml-order",
        "options": {},
        "httpMethod": "POST",
        "responseMode": "responseNode"
      },
      "typeVersion": 2.1
    },
    {
      "id": "529760e9-6325-40e6-a399-c0f52ed1cea4",
      "name": "Validate webhook",
      "type": "n8n-nodes-base.code",
      "position": [
        208,
        0
      ],
      "parameters": {
        "jsCode": "const REQUIRED_FIELDS = ['resource', 'topic', 'user_id'];\nconst RESOURCE_PATTERN = /^\\/orders\\/(\\d+)$/;\nconst SUPPORTED_TOPICS = ['orders_v2'];\n\nconst results = [];\n\nfor (const item of $input.all()) {\n    // n8n Webhook node envuelve el body en item.json.body; cuando el código\n    // se invoca con un objeto plano (tests directos) los campos viven al raíz.\n    // Soportamos ambos formatos.\n    const raw = item.json;\n    const payload = (raw && typeof raw.body === 'object' && raw.body !== null) ? raw.body : raw;\n    const errors = [];\n\n    // 1. Campos obligatorios presentes\n    for (const field of REQUIRED_FIELDS) {\n        if (payload[field] === undefined || payload[field] === null) {\n            errors.push(`missing_field:${field}`);\n        }\n    }\n\n    // 2. resource debe matchear el formato /orders/{id}\n    let order_id = null;\n    if (payload.resource) {\n        const match = String(payload.resource).match(RESOURCE_PATTERN);\n        if (match) {\n            order_id = match[1];\n        } else {\n            errors.push(`invalid_resource_format:${payload.resource}`);\n        }\n    }\n\n    // 3. topic debe ser uno de los soportados\n    if (payload.topic && !SUPPORTED_TOPICS.includes(payload.topic)) {\n        errors.push(`unsupported_topic:${payload.topic}`);\n    }\n\n    // 4. user_id debe ser numérico\n    if (payload.user_id !== undefined && !Number.isFinite(Number(payload.user_id))) {\n        errors.push(`invalid_user_id:${payload.user_id}`);\n    }\n\n    results.push({\n        json: {\n            valid: errors.length === 0,\n            errors,\n            webhook: payload,\n            order_id,\n            channel: 'mercadolibre',\n            received_at: new Date().toISOString(),\n        }\n    });\n}\n\nreturn results;"
      },
      "typeVersion": 2
    },
    {
      "id": "a70d0a0a-3744-4dbf-97a3-a2c6989f15c6",
      "name": "Valid?",
      "type": "n8n-nodes-base.if",
      "position": [
        416,
        0
      ],
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 3,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "strict"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "e8f92719-40a8-4053-aeab-a44adc8bc8f9",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ $json.valid }}",
              "rightValue": ""
            }
          ]
        }
      },
      "typeVersion": 2.3
    },
    {
      "id": "9e28ae80-32db-480a-bb96-a8d6e6ddcd55",
      "name": "Audit validation_failed",
      "type": "n8n-nodes-base.postgres",
      "position": [
        624,
        96
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "=null, {{ 'validation_failed' }}, {{ 'warning' }}, {{ 'webhook' }}, {{ 'Webhook invalido' }}, {{ JSON.stringify({errors: $('Validate webhook').item.json.errors}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "076a6c6c-a521-4ecf-a88f-c6333b285813",
      "name": "Respond 400",
      "type": "n8n-nodes-base.respondToWebhook",
      "position": [
        832,
        96
      ],
      "parameters": {
        "options": {
          "responseCode": 400
        },
        "respondWith": "json",
        "responseBody": "{ \"error\": \"invalid_payload\", \"details\": \"{{ $json.errors }}\" }"
      },
      "typeVersion": 1.5
    },
    {
      "id": "dec166bc-da5c-4abd-bcab-cbede1d9df71",
      "name": "Enrich from mock",
      "type": "n8n-nodes-base.httpRequest",
      "position": [
        624,
        -96
      ],
      "parameters": {
        "url": "=http://mock-marketplace/orders/{{ $(\"Validate webhook\").item.json.order_id }}",
        "options": {}
      },
      "typeVersion": 4.4
    },
    {
      "id": "17cdfa18-1c74-4fa2-841d-abce9e32961d",
      "name": "Normalize ML order",
      "type": "n8n-nodes-base.code",
      "position": [
        832,
        -96
      ],
      "parameters": {
        "jsCode": "// =============================================================================\n// normalize-ml-order.js\n// -----------------------------------------------------------------------------\n// Pegar en un nodo \"Code\" de n8n (modo: \"Run Once for All Items\") ubicado\n// después del HTTP Request que trae /orders/{id} desde mock-marketplace.\n//\n// Función:\n//   - Mapea la estructura cruda de Mercado Libre al modelo canónico\n//   - Calcula SHA-256 de email y teléfono (para dedupe futuro sin exponer PII)\n//   - Genera pseudonym determinístico por (channel, external_id)\n//   - Normaliza el estado del pedido al dominio del CHECK constraint en tfi.orders\n//   - Devuelve un objeto listo para los nodos Postgres siguientes\n//\n// Input por item (lo que devuelve el HTTP Request):\n//   { id, status, buyer, order_items, total_amount, currency_id, ... }\n//\n// Output por item:\n//   {\n//     customer: { external_id, channel, pseudonym, full_name, email, phone,\n//                 email_hash, phone_hash },\n//     order:    { external_id, channel, status, total_amount, currency,\n//                 source_created_at, raw_payload, normalization_version },\n//     items:    [{ sku, product_name, quantity, unit_price, delivery_status }],\n//     meta:     { normalization_ms, applied_rules }\n//   }\n// =============================================================================\n\nconst crypto = require('crypto');\n\nconst NORMALIZATION_VERSION = 'v1';\nconst CHANNEL = 'mercadolibre';\n\n// --- Mapeo de estados ML → modelo canónico -----------------------------------\n// El CHECK constraint en tfi.orders sólo permite estos valores:\n//   created, pending_payment, paid, preparing, shipped, delivered, cancelled,\n//   refunded, error\nconst STATUS_MAP = {\n    // ML real puede mandar: confirmed, payment_required, payment_in_process,\n    // partially_paid, paid, partially_refunded, pending_cancel, cancelled,\n    // invalid, ...\n    confirmed:            'created',\n    payment_required:     'pending_payment',\n    payment_in_process:   'pending_payment',\n    partially_paid:       'pending_payment',\n    paid:                 'paid',\n    preparing:            'preparing',  // estado custom si lo usamos\n    shipped:              'shipped',\n    delivered:            'delivered',\n    partially_refunded:   'refunded',\n    refunded:             'refunded',\n    pending_cancel:       'cancelled',\n    cancelled:            'cancelled',\n    invalid:              'error',\n};\n\n// --- Mapeo de shipping status ML → delivery_status canónico ------------------\nconst DELIVERY_STATUS_MAP = {\n    pending:        'pending',\n    handling:       'preparing',\n    ready_to_ship:  'preparing',\n    shipped:        'shipped',\n    delivered:      'delivered',\n    not_delivered: 'pending',\n    cancelled:      'cancelled',\n    returned:       'returned',\n};\n\n// --- Helpers -----------------------------------------------------------------\nfunction sha256(value) {\n    if (value === null || value === undefined || value === '') return null;\n    return crypto.createHash('sha256').update(String(value).toLowerCase().trim()).digest('hex');\n}\n\nfunction makePseudonym(channel, externalId) {\n    // Determinístico: misma combinación siempre da el mismo pseudonym.\n    // Sirve para deduplicar entre webhooks repetidos y mantener trazabilidad\n    // sin exponer el id real al LLM.\n    const hash = crypto\n        .createHash('sha256')\n        .update(`${channel}:${externalId}`)\n        .digest('hex')\n        .slice(0, 12);\n    return `cust_${hash}`;\n}\n\nfunction buildPhoneString(phone) {\n    if (!phone) return null;\n    const area = phone.area_code || '';\n    const num = phone.number || '';\n    return area && num ? `${area}${num}` : (num || null);\n}\n\nfunction normalizeStatus(mlStatus, mlStatusDetail) {\n    const mapped = STATUS_MAP[mlStatus];\n    if (mapped) return mapped;\n    // Status desconocido: lo registramos en applied_rules y devolvemos 'error'\n    // para que un humano lo revise; mejor que omitir o adivinar.\n    return 'error';\n}\n\nfunction deriveDeliveryStatus(orderStatus, shippingStatus) {\n    if (orderStatus === 'cancelled') return 'cancelled';\n    if (shippingStatus && DELIVERY_STATUS_MAP[shippingStatus]) {\n        return DELIVERY_STATUS_MAP[shippingStatus];\n    }\n    return 'pending';\n}\n\n// --- Main loop ---------------------------------------------------------------\nconst out = [];\n\nfor (const item of $input.all()) {\n    const t0 = Date.now();\n    const ml = item.json;\n    const appliedRules = [];\n\n    // Customer\n    const buyer = ml.buyer || {};\n    const fullName = [buyer.first_name, buyer.last_name].filter(Boolean).join(' ') || null;\n    const phone = buildPhoneString(buyer.phone);\n\n    // D1 (v1.3): si el buyer_id viene nulo, se genera un identificador sustituto\n    // determinístico `synthetic:{channel}:{order.external_id}`. Es reproducible\n    // (mismo pedido reingresado hace UPSERT sobre la misma fila) y trazable\n    // (el prefijo permite filtrar y contar sustituciones). Preserva la\n    // integridad referencial (customers.external_id NOT NULL) sin bloquear\n    // el pedido, según §4.6 del TFI corregido.\n    let externalCustomerId = buyer.id ? String(buyer.id) : null;\n    if (!externalCustomerId) {\n        externalCustomerId = `synthetic:${CHANNEL}:${ml.id}`;\n        appliedRules.push('warn:buyer_id_missing_substituted');\n    }\n\n    const customer = {\n        external_id: externalCustomerId,\n        channel: CHANNEL,\n        pseudonym: makePseudonym(CHANNEL, externalCustomerId),\n        full_name: fullName,\n        email: buyer.email || null,\n        phone,\n        email_hash: sha256(buyer.email),\n        phone_hash: sha256(phone),\n    };\n\n    // Order\n    const status = normalizeStatus(ml.status, ml.status_detail);\n    if (status === 'error') {\n        appliedRules.push(`warn:unmapped_status:${ml.status}`);\n    }\n\n    const order = {\n        external_id: String(ml.id),\n        channel: CHANNEL,\n        status,\n        total_amount: Number(ml.total_amount) || 0,\n        currency: ml.currency_id || 'ARS',\n        source_created_at: ml.date_created || new Date().toISOString(),\n        raw_payload: ml,\n        normalization_version: NORMALIZATION_VERSION,\n    };\n\n    // Items\n    const shippingStatus = ml.shipping && ml.shipping.status;\n    const deliveryStatus = deriveDeliveryStatus(status, shippingStatus);\n\n    const items = (ml.order_items || []).map((oi) => {\n        const itm = oi.item || {};\n        return {\n            sku: itm.seller_custom_field || itm.id || null,\n            product_name: itm.title || '(sin título)',\n            quantity: Number(oi.quantity) || 1,\n            unit_price: Number(oi.unit_price) || 0,\n            delivery_status: deliveryStatus,\n            metadata: {\n                ml_item_id: itm.id || null,\n                category_id: itm.category_id || null,\n                warranty: itm.warranty || null,\n                sale_fee: oi.sale_fee || null,\n            },\n        };\n    });\n\n    if (items.length === 0) {\n        appliedRules.push('warn:no_items');\n    }\n\n    // v1.3: raw_event_id y ack_at vienen del nodo Persist raw event ML\n    // (§4.1.1 del TFI corregido). El ack_at se persiste en tfi.orders y la\n    // ventana received_at→ack_at debe quedar por debajo de 500 ms.\n    const rawEvt = $('Persist raw event ML').item.json;\n\n    out.push({\n        json: {\n            customer,\n            order,\n            items,\n            raw_event_id: rawEvt.raw_event_id,\n            ack_at: rawEvt.ack_at,\n            meta: {\n                normalization_ms: Date.now() - t0,\n                applied_rules: appliedRules,\n                normalization_version: NORMALIZATION_VERSION,\n            },\n        },\n    });\n}\n\nreturn out;\n"
      },
      "typeVersion": 2
    },
    {
      "id": "d7a2e911-8e1c-4fdc-b33f-4bc5efb3d753",
      "name": "Check idempotency",
      "type": "n8n-nodes-base.postgres",
      "position": [
        1040,
        -96
      ],
      "parameters": {
        "query": "SELECT\n    o.id                  AS existing_order_id,\n    o.status              AS existing_status,\n    o.received_at         AS existing_received_at,\n    (o.id IS NOT NULL)    AS already_exists\nFROM (SELECT 1) AS _dummy\nLEFT JOIN tfi.orders o\n    ON o.external_id = $1\n   AND o.channel     = $2;",
        "options": {
          "queryReplacement": "={{ $('Route to canonical').item.json.order.external_id }}, {{ $('Route to canonical').item.json.order.channel }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "7fd5aa6f-5e6a-4686-b03d-e7a3d14c29a4",
      "name": "If Duplicate?",
      "type": "n8n-nodes-base.if",
      "position": [
        1248,
        -96
      ],
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 3,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "strict"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "42a30a8b-21bc-4792-9ffd-032d75c78b41",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ $json.already_exists }}",
              "rightValue": ""
            }
          ]
        }
      },
      "typeVersion": 2.3
    },
    {
      "id": "cf822d92-ff8a-442d-8bcd-01724998dcf3",
      "name": "Audit duplicate",
      "type": "n8n-nodes-base.postgres",
      "position": [
        1456,
        -192
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "={{ $('Check idempotency').item.json.existing_order_id }}, {{ 'duplicate_detected' }}, {{ 'info' }}, {{ 'persister' }}, {{ 'Webhook reenviado de orden ya procesada' }}, {{ JSON.stringify({external_id: $('Route to canonical').item.json.order.external_id}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "38051465-c8c8-4b4d-b7fe-488bc9fa6a93",
      "name": "Upsert customer",
      "type": "n8n-nodes-base.postgres",
      "position": [
        1456,
        0
      ],
      "parameters": {
        "query": "INSERT INTO tfi.customers (\n      external_id, channel, pseudonym,\n      full_name, email, phone,\n      email_hash, phone_hash\n  )\n  VALUES ($1, $2, $3, $4, $5, $6, $7, $8)\n  ON CONFLICT (external_id, channel) DO UPDATE SET\n      full_name  = COALESCE(EXCLUDED.full_name,  tfi.customers.full_name),\n      email      = COALESCE(EXCLUDED.email,      tfi.customers.email),\n      phone      = COALESCE(EXCLUDED.phone,      tfi.customers.phone),\n      email_hash = COALESCE(EXCLUDED.email_hash, tfi.customers.email_hash),\n      phone_hash = COALESCE(EXCLUDED.phone_hash, tfi.customers.phone_hash),\n      updated_at = now()\n  RETURNING id AS customer_id, pseudonym;",
        "options": {
          "queryReplacement": "=={{ $('Route to canonical').item.json.customer.external_id }}, {{ $('Route to canonical').item.json.customer.channel }}, {{ $('Route to canonical').item.json.customer.pseudonym }}, {{ $('Route to canonical').item.json.customer.full_name }}, {{ $('Route to canonical').item.json.customer.email }}, {{ $('Route to canonical').item.json.customer.phone }}, {{ $('Route to canonical').item.json.customer.email_hash }}, {{ $('Route to canonical').item.json.customer.phone_hash }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "ade471f3-0318-4153-a1df-ff67ca99ed5a",
      "name": "Insert Order",
      "type": "n8n-nodes-base.postgres",
      "position": [
        1664,
        0
      ],
      "parameters": {
        "query": "INSERT INTO tfi.orders (\n    external_id, channel, customer_id,\n    status, total_amount, currency,\n    source_created_at, raw_payload, normalization_version,\n    received_at, ack_at\n)\nVALUES (\n    $1, $2, $3,\n    $4, $5, $6,\n    $7, $8::jsonb, $9,\n    $10::timestamptz, $10::timestamptz\n)\nON CONFLICT (external_id, channel) DO NOTHING\nRETURNING id AS order_id, received_at, ack_at;",
        "options": {
          "queryReplacement": "={{ $(\"Route to canonical\").item.json.order.external_id }}, {{ $(\"Route to canonical\").item.json.order.channel }}, {{ $(\"Upsert customer\").item.json.customer_id }}, {{ $(\"Route to canonical\").item.json.order.status }}, {{ $(\"Route to canonical\").item.json.order.total_amount }}, {{ $(\"Route to canonical\").item.json.order.currency }}, {{ $(\"Route to canonical\").item.json.order.source_created_at }}, {{ JSON.stringify($(\"Route to canonical\").item.json.order.raw_payload) }}, {{ $(\"Route to canonical\").item.json.order.normalization_version }}, {{ $(\"Route to canonical\").item.json.ack_at }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "437ca9cc-8ca8-4540-8a0e-42fdb3b50a88",
      "name": "Split Items",
      "type": "n8n-nodes-base.code",
      "position": [
        1872,
        0
      ],
      "parameters": {
        "jsCode": "// Split Items: convierte items[] del Normalize node en items individuales\n// para que el siguiente Postgres node (Insert Items) los procese uno por uno.\n\nconst norm = $('Route to canonical').item.json;\nconst items = norm.items || [];\nconst orderId = $('Insert Order').item.json.order_id;\n\nreturn items.map(it => ({\n  json: {\n    order_id: orderId,\n    sku: it.sku,\n    product_name: it.product_name,\n    quantity: it.quantity,\n    unit_price: it.unit_price,\n    delivery_status: it.delivery_status,\n    metadata: it.metadata || {},\n  }\n}));"
      },
      "typeVersion": 2
    },
    {
      "id": "3072c087-3e00-4acc-a049-14e489d62c2f",
      "name": "Insert Items",
      "type": "n8n-nodes-base.postgres",
      "position": [
        2080,
        0
      ],
      "parameters": {
        "query": "INSERT INTO tfi.order_items (\n      order_id, sku, product_name,\n      quantity, unit_price, delivery_status, metadata\n  )\n  VALUES (\n      $1, $2, $3,\n      $4, $5, $6, $7::jsonb\n  )\n  RETURNING id AS item_id;",
        "options": {
          "queryReplacement": "={{ $('Insert Order').item.json.order_id }}, {{ $json.sku }}, {{ $json.product_name }}, {{ $json.quantity }}, {{ $json.unit_price }}, {{ $json.delivery_status }}, {{ JSON.stringify($json.metadata || {}) }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "b0575570-84b8-48a5-97cf-51a0bd5033fb",
      "name": "Build LLM prompt",
      "type": "n8n-nodes-base.code",
      "position": [
        2288,
        0
      ],
      "parameters": {
        "jsCode": "const PROMPT_VERSION = 'v1';\n\n// Hiperparámetros del prompts/v1.md (versión inicial)\nconst HYPERPARAMS = {\n    temperature: 0.4,\n    top_p: 0.9,\n    max_tokens: 220,\n    response_format: { type: 'json_object' },\n    seed: 42,\n};\n\nconst SYSTEM_MESSAGE = `Sos un asistente de comunicación post-venta para una PyME argentina que vende\nneumáticos en Mercado Libre. Tu única tarea es redactar mensajes cortos y\ncordiales para enviar al comprador después de un cambio en el estado de su\npedido.\n\nREGLAS ESTRICTAS:\n1. Solo usar información que esté en el contexto JSON que te entrego.\n   Si un dato falta, no lo inventes ni lo asumas: omitilo del mensaje.\n2. No incluyas tu razonamiento, disculpas por restricciones, ni meta-comentarios.\n3. No mencionés a \"OpenAI\", \"IA\", \"modelo de lenguaje\" ni similares.\n4. Tono: argentino neutro, cordial pero no informal. Usá \"vos\", no \"tú\" ni \"usted\".\n5. Longitud: máximo 4 oraciones, idealmente 2-3. Sin emojis salvo que el contexto\n   los pida explícitamente.\n6. No prometas tiempos de entrega, descuentos, ni acciones que el contexto no\n   confirme.\n7. Si el estado es \"error\" o el contexto está incompleto, generá un mensaje\n   genérico de \"estamos revisando tu pedido\" sin inventar detalles.\n\nOUTPUT:\nDevolvé exclusivamente un objeto JSON válido con esta estructura:\n{\n  \"message\": \"texto del mensaje, listo para enviar\",\n  \"tone\": \"informativo\" | \"cordial\" | \"disculpa\" | \"celebratorio\",\n  \"confidence\": número entre 0 y 1\n}\n\nconfidence refleja qué tan completa es la información del contexto.\nSi tuviste que omitir datos importantes, bajá la confidence.`;\n\n// --- Helpers -----------------------------------------------------------------\nfunction getPrimaryProductName(items) {\n    if (!items || items.length === 0) return null;\n    // Tomamos el item de mayor monto (más representativo del pedido)\n    const sorted = [...items].sort(\n        (a, b) => (Number(b.unit_price) * Number(b.quantity)) -\n                  (Number(a.unit_price) * Number(a.quantity))\n    );\n    return sorted[0].product_name;\n}\n\nfunction buildContext(input) {\n    // SOLO estos campos pueden llegar al LLM.\n    // No incluir nunca: full_name, email, phone, doc_number, billing_info, etc.\n    const ctx = {\n        customer_pseudonym: input.customer.pseudonym,\n        order_status: input.order.status,\n        items_count: (input.items || []).length,\n        primary_product_name: getPrimaryProductName(input.items),\n        channel: input.order.channel,\n        total_amount: input.order.total_amount,\n        currency: input.order.currency,\n        source_created_at: input.order.source_created_at,\n    };\n\n    // Defensa: lanzar error si alguna PII se coló accidentalmente\n    const pii_keys = ['full_name', 'email', 'phone', 'doc_number', 'address', 'first_name', 'last_name'];\n    const ctx_str = JSON.stringify(ctx).toLowerCase();\n    for (const key of pii_keys) {\n        if (ctx_str.includes(`\"${key}\"`)) {\n            throw new Error(`PII leak detected: campo ${key} no debe llegar al LLM`);\n        }\n    }\n\n    return ctx;\n}\n\nfunction buildUserMessage(ctx) {\n    return `Contexto del pedido:\n- Pseudónimo del cliente: ${ctx.customer_pseudonym}\n- Estado normalizado: ${ctx.order_status}\n- Cantidad de productos: ${ctx.items_count}\n- Producto principal: ${ctx.primary_product_name || '(no especificado)'}\n- Canal de venta: ${ctx.channel}\n- Monto total: ${ctx.total_amount} ${ctx.currency}\n- Fecha del pedido: ${ctx.source_created_at}\n\nGenerá el mensaje según las reglas del system.`;\n}\n\n// --- Main --------------------------------------------------------------------\nconst input = $('Route to canonical').item.json;\n\nif (!input.customer || !input.order) {\n    throw new Error('Input inválido: faltan customer u order. Verificar el normalizador.');\n}\n\nconst t_build_start = Date.now();\nconst context = buildContext(input);\nconst userMessage = buildUserMessage(context);\n\nreturn {\n    json: {\n        // Identificadores que vamos a necesitar después\n        order_id: input.order_id,\n        customer_id: input.customer_id,\n        prompt_version: PROMPT_VERSION,\n\n        // Mensajes para el nodo OpenAI (algunos nodos los esperan en .messages,\n        // otros directamente; ajustar según la versión del nodo OpenAI)\n        messages: [\n            { role: 'system', content: SYSTEM_MESSAGE },\n            { role: 'user',   content: userMessage },\n        ],\n\n        // Hiperparámetros — el nodo OpenAI permite override con estos campos\n        model: 'gpt-4o-mini',\n        ...HYPERPARAMS,\n\n        // Métricas internas\n        prompt_build_ms: Date.now() - t_build_start,\n        // Conservamos el contexto enviado para auditoría posterior\n        sent_context: context,\n        // Para que el parser sepa cuándo arrancó la llamada\n        llm_call_started_at: new Date().toISOString(),\n    },\n};"
      },
      "typeVersion": 2
    },
    {
      "id": "80e2500a-465d-4575-b63b-76ded946f386",
      "name": "Call OpenAI",
      "type": "@n8n/n8n-nodes-langchain.openAi",
      "onError": "continueErrorOutput",
      "position": [
        2496,
        0
      ],
      "parameters": {
        "modelId": {
          "__rl": true,
          "mode": "list",
          "value": "gpt-4o-mini",
          "cachedResultName": "GPT-4O-MINI"
        },
        "options": {
          "maxTokens": 220,
          "temperature": 0.4
        },
        "responses": {
          "values": [
            {
              "role": "system",
              "content": "={{ $json.messages[0].content }}"
            },
            {
              "content": "={{ $json.messages[1].content }}"
            }
          ]
        },
        "builtInTools": {}
      },
      "credentials": {
        "openAiApi": {
          "id": "xVEvgE6fo3hvouhY",
          "name": "OpenAI account"
        }
      },
      "typeVersion": 2.1
    },
    {
      "id": "bcceee11-7dc1-463b-b778-064575271f0c",
      "name": "Parse LLM Response",
      "type": "n8n-nodes-base.code",
      "position": [
        2752,
        0
      ],
      "parameters": {
        "jsCode": "// =============================================================================\n// parse-llm-response.js\n// -----------------------------------------------------------------------------\n// Pegar en un nodo \"Code\" (modo: \"Run Once for Each Item\") DESPUÉS del nodo\n// OpenAI/Anthropic.\n//\n// Función:\n//   - Extrae el texto generado y la metadata de uso (tokens, latencia)\n//   - Valida que sea JSON parseable con la shape esperada\n//   - Calcula cost_usd usando el pricing del proveedor\n//   - Devuelve un item listo para INSERT en tfi.ai_notifications,\n//     o un item con use_fallback=true para que el siguiente nodo IF\n//     redirija al fallback-template.js\n//\n// Pricing por 1M tokens (actualizar cuando cambien las tarifas).\n// Fuente: https://openai.com/pricing y https://www.anthropic.com/pricing\n// =============================================================================\n\nconst PRICING = {\n    openai: {\n        'gpt-4o-mini':  { input: 0.150, output: 0.600 },  // USD por 1M tokens\n        'gpt-4o':       { input: 2.500, output: 10.000 },\n    },\n    anthropic: {\n        'claude-haiku-4-5':   { input: 1.000, output: 5.000 },  // placeholder, ajustar\n        'claude-haiku-3-5':   { input: 0.800, output: 4.000 },\n    },\n};\n\nconst MIN_CONFIDENCE = 0.3;            // confidence < este → fallback\nconst MAX_MESSAGE_LENGTH = 600;        // chars, defensa contra respuestas muy largas\nconst MIN_MESSAGE_LENGTH = 20;         // chars, evita respuestas vacías/triviales\n\n// --- Helpers -----------------------------------------------------------------\nfunction calculateCostUSD(provider, model, promptTokens, completionTokens) {\n    const tier = PRICING[provider] && PRICING[provider][model];\n    if (!tier) return null; // pricing desconocido → guardamos NULL en DB\n    const inputCost  = (promptTokens     || 0) * tier.input  / 1_000_000;\n    const outputCost = (completionTokens || 0) * tier.output / 1_000_000;\n    return Number((inputCost + outputCost).toFixed(6));\n}\n\nfunction extractTextAndUsage(llmRaw) {\n    // Soporta tanto OpenAI como Anthropic en estructuras nativas, además del\n    // wrapper que aplica el nodo OpenAI de n8n.\n\n    // n8n OpenAI node devuelve a veces { message: { content: \"...\" } } o\n    // { content: \"...\" } o el formato crudo de la API.\n    if (typeof llmRaw === 'string') {\n        return { text: llmRaw, usage: {} };\n    }\n\n    // Formato crudo de OpenAI Chat Completions API\n    if (llmRaw.choices && Array.isArray(llmRaw.choices) && llmRaw.choices[0]) {\n        const choice = llmRaw.choices[0];\n        const text = (choice.message && choice.message.content) ||\n                     choice.text ||\n                     '';\n        return { text, usage: llmRaw.usage || {} };\n    }\n\n    // Formato crudo de Anthropic Messages API\n    if (llmRaw.content && Array.isArray(llmRaw.content) && llmRaw.content[0]) {\n        const text = llmRaw.content[0].text || '';\n        const usage = llmRaw.usage ? {\n            prompt_tokens:     llmRaw.usage.input_tokens,\n            completion_tokens: llmRaw.usage.output_tokens,\n        } : {};\n        return { text, usage };\n    }\n\n    // Algunos wrappers de n8n\n    if (llmRaw.message && llmRaw.message.content) {\n        return { text: llmRaw.message.content, usage: llmRaw.usage || {} };\n    }\n    if (typeof llmRaw.content === 'string') {\n        return { text: llmRaw.content, usage: llmRaw.usage || {} };\n    }\n    if (typeof llmRaw.text === 'string') {\n        return { text: llmRaw.text, usage: llmRaw.usage || {} };\n    }\n\n    return { text: '', usage: {} };\n}\n\nfunction parseAndValidate(text) {\n    const out = { valid: false, parsed: null, errors: [] };\n\n    if (!text || typeof text !== 'string') {\n        out.errors.push('empty_response');\n        return out;\n    }\n\n    let parsed;\n    try {\n        // Algunos modelos pueden devolver el JSON envuelto en backticks; los limpiamos\n        const cleaned = text.replace(/^```(?:json)?\\s*/i, '').replace(/\\s*```$/, '').trim();\n        parsed = JSON.parse(cleaned);\n    } catch (e) {\n        out.errors.push(`invalid_json:${e.message}`);\n        return out;\n    }\n\n    if (typeof parsed.message !== 'string') {\n        out.errors.push('missing_field:message');\n    } else {\n        if (parsed.message.length < MIN_MESSAGE_LENGTH) {\n            out.errors.push(`message_too_short:${parsed.message.length}`);\n        }\n        if (parsed.message.length > MAX_MESSAGE_LENGTH) {\n            out.errors.push(`message_too_long:${parsed.message.length}`);\n        }\n    }\n\n    if (typeof parsed.tone !== 'string') {\n        out.errors.push('missing_field:tone');\n    }\n\n    if (typeof parsed.confidence !== 'number' ||\n        parsed.confidence < 0 ||\n        parsed.confidence > 1) {\n        out.errors.push('invalid_field:confidence');\n    } else if (parsed.confidence < MIN_CONFIDENCE) {\n        out.errors.push(`low_confidence:${parsed.confidence}`);\n    }\n\n    out.valid = out.errors.length === 0;\n    out.parsed = parsed;\n    return out;\n}\n\n// --- Main --------------------------------------------------------------------\nconst input = $json;\nconst t_parse_start = Date.now();\n\n// Identificadores y metadata heredados del build-llm-prompt\nconst order_id       = input.order_id;\nconst customer_id    = input.customer_id;\nconst prompt_version = input.prompt_version || 'v1';\nconst provider       = input.provider || 'openai';\nconst model          = input.model || 'gpt-4o-mini';\nconst startedAt      = input.llm_call_started_at;\n\n// La respuesta real puede estar en distintos lados según cómo se conectó el nodo\nconst llmRaw = input.response || input.data || input.llm || input;\n\nconst { text, usage } = extractTextAndUsage(llmRaw);\nconst validation = parseAndValidate(text);\n\nconst promptTokens     = usage.prompt_tokens     || usage.input_tokens     || null;\nconst completionTokens = usage.completion_tokens || usage.output_tokens    || null;\nconst costUSD          = calculateCostUSD(provider, model, promptTokens, completionTokens);\n\nconst latencyMs = startedAt\n    ? (Date.now() - new Date(startedAt).getTime())\n    : null;\n\nif (validation.valid) {\n    // Camino feliz: usar el mensaje del LLM\n    return {\n        json: {\n            order_id,\n            customer_id,\n            use_fallback: false,\n\n            provider,\n            model,\n            prompt_version,\n\n            usage: {\n                prompt_tokens: promptTokens,\n                completion_tokens: completionTokens,\n            },\n            cost_usd: costUSD,\n            latency_ms: latencyMs,\n\n            message_text: validation.parsed.message,\n            tone: validation.parsed.tone,\n            confidence: validation.parsed.confidence,\n            is_fallback: false,\n            message_status: 'validated',\n            error_message: null,\n\n            audit_event: {\n                event_type: 'llm_call_ok',\n                severity: 'info',\n                component: 'llm',\n                message: `LLM ${provider}/${model} respondió OK (confidence=${validation.parsed.confidence})`,\n                payload: {\n                    prompt_version,\n                    tokens_prompt: promptTokens,\n                    tokens_completion: completionTokens,\n                    cost_usd: costUSD,\n                    latency_ms: latencyMs,\n                    confidence: validation.parsed.confidence,\n                    tone: validation.parsed.tone,\n                },\n            },\n        },\n    };\n}\n\n// Respuesta inválida: marcar para que el siguiente IF node rutee al fallback\nreturn {\n    json: {\n        order_id,\n        customer_id,\n        use_fallback: true,\n        fallback_reason: validation.errors.join(','),\n\n        // Preservamos lo que sí pudimos extraer para auditoría\n        provider,\n        model,\n        prompt_version,\n        cost_usd: costUSD,       // pagamos los tokens igual aunque sea inválida\n        latency_ms: latencyMs,\n        raw_response: text,       // crudo para análisis posterior\n        validation_errors: validation.errors,\n\n        // Necesario para que el nodo fallback construya bien (mantiene shape)\n        order: input.order || null,\n        customer: input.customer || null,\n        items: input.items || null,\n\n        audit_event: {\n            event_type: 'llm_call_failed',\n            severity: 'warning',\n            component: 'llm',\n            message: `LLM ${provider}/${model} devolvió respuesta inválida: ${validation.errors.join(', ')}`,\n            payload: {\n                prompt_version,\n                errors: validation.errors,\n                cost_usd: costUSD,\n                latency_ms: latencyMs,\n                raw_excerpt: text.slice(0, 200),\n            },\n        },\n    },\n};\n"
      },
      "typeVersion": 2
    },
    {
      "id": "36e48643-8980-44af-b099-af94d1e045b9",
      "name": "Use Fallback?",
      "type": "n8n-nodes-base.if",
      "position": [
        2960,
        0
      ],
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 3,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "strict"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "e5bf4d71-af08-421d-b663-4ddce9421192",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ $json.use_fallback }}",
              "rightValue": ""
            }
          ]
        }
      },
      "typeVersion": 2.3
    },
    {
      "id": "3bac0f02-6ded-4460-b28c-08226218aecc",
      "name": "Fallback template",
      "type": "n8n-nodes-base.code",
      "position": [
        3168,
        -96
      ],
      "parameters": {
        "jsCode": "// =============================================================================\n// fallback-template.js\n// -----------------------------------------------------------------------------\n// Pegar en un nodo \"Code\" (modo: \"Run Once for Each Item\") en la rama de\n// degradación funcional del workflow.\n//\n// Se invoca cuando:\n//   - El LLM no responde dentro del timeout configurado\n//   - La respuesta del LLM no parsea como JSON\n//   - La respuesta no cumple la shape esperada (message/tone/confidence)\n//   - El confidence devuelto es muy bajo (< 0.3)\n//\n// Produce un mensaje basado en plantilla estática por estado, garantizando\n// que el cliente reciba algo aunque el componente generativo no funcione.\n// Cap 5.8 del documento — degradación funcional controlada.\n// =============================================================================\n\nconst TEMPLATE_VERSION = 'template-v1';\n\n// Plantillas por estado del modelo canónico (alineadas con CHECK constraint\n// de tfi.orders.status). Texto: argentino neutro, mismo tono que el prompt v1.\nconst TEMPLATES = {\n    created: {\n        message: 'Recibimos tu compra. Te avisamos en cuanto se confirme el pago.',\n        tone: 'informativo',\n    },\n    pending_payment: {\n        message: 'Estamos esperando la confirmación del pago de tu compra. Si ya pagaste, no te preocupes — en algunos casos puede demorar unos minutos.',\n        tone: 'informativo',\n    },\n    paid: {\n        message: 'Recibimos el pago de tu compra. Estamos preparando tu pedido para el envío. Gracias por elegirnos.',\n        tone: 'cordial',\n    },\n    preparing: {\n        message: 'Tu pedido se está preparando. Te avisamos en cuanto lo despachemos.',\n        tone: 'informativo',\n    },\n    shipped: {\n        message: 'Tu pedido fue despachado. Vas a recibirlo en los próximos días según el método de envío que elegiste.',\n        tone: 'informativo',\n    },\n    delivered: {\n        message: 'Confirmamos la entrega de tu pedido. ¡Gracias por tu compra! Si necesitás algo, escribinos.',\n        tone: 'celebratorio',\n    },\n    cancelled: {\n        message: 'Tu pedido fue cancelado. Si fue un error o querés más información, contactanos así lo resolvemos.',\n        tone: 'disculpa',\n    },\n    refunded: {\n        message: 'Se procesó el reembolso de tu pedido. Puede demorar unos días en verse reflejado según tu medio de pago.',\n        tone: 'disculpa',\n    },\n    error: {\n        message: 'Estamos revisando algunos detalles de tu pedido. Te contactamos en cuanto tengamos novedades.',\n        tone: 'informativo',\n    },\n};\n\n// Plantilla absolutamente genérica si el estado tampoco existe en el map.\n// No debería llegar acá si el normalizador funcionó (mapea estados desconocidos\n// a 'error'), pero es defensa en profundidad.\nconst ULTIMATE_FALLBACK = {\n    message: 'Recibimos tu compra. Te contactamos en cuanto haya novedades sobre tu pedido.',\n    tone: 'informativo',\n};\n\nconst input = $json;\nconst status = input.order && input.order.status;\nconst reason = input.fallback_reason || 'unknown';\n\nconst template = TEMPLATES[status] || ULTIMATE_FALLBACK;\n\nreturn {\n    json: {\n        order_id: input.order_id,\n        customer_id: input.customer_id,\n\n        // Provider especial 'template' coherente con el CHECK constraint\n        // de tfi.ai_notifications (provider='template' ↔ is_fallback=true)\n        provider: 'template',\n        model: null,\n        prompt_version: null,\n\n        // Sin tokens ni costo porque no fue llamada externa\n        usage: { prompt_tokens: null, completion_tokens: null },\n        cost_usd: 0,\n        latency_ms: 0,\n\n        // Output con la misma shape que devolvería el LLM, así el INSERT\n        // posterior es uniforme y no requiere branching\n        message_text: template.message,\n        tone: template.tone,\n        confidence: 1.0, // plantilla es 100% predecible\n        is_fallback: true,\n        message_status: 'validated',\n\n        // Telemetría: por qué se cayó al fallback\n        fallback_reason: reason,\n        template_version: TEMPLATE_VERSION,\n        applied_template_status: status,\n\n        // Para audit_log\n        audit_event: {\n            event_type: 'fallback_triggered',\n            severity: 'warning',\n            component: 'llm',\n            message: `Degradación a plantilla por ${reason}`,\n            payload: { fallback_reason: reason, status, template_version: TEMPLATE_VERSION },\n        },\n    },\n};\n"
      },
      "typeVersion": 2
    },
    {
      "id": "635acbf3-47de-4140-8ba2-c1ea55db2ddd",
      "name": "Get customer phone",
      "type": "n8n-nodes-base.postgres",
      "position": [
        3376,
        96
      ],
      "parameters": {
        "query": " SELECT\n      c.phone,\n      c.full_name\n  FROM tfi.customers  c\n  JOIN tfi.orders     o ON o.customer_id = c.id\n  WHERE o.id = $1::uuid;",
        "options": {
          "queryReplacement": "{{ $json.order_id }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "8618f982-b077-4070-aab0-96f3a621c346",
      "name": "Insert ai_notification",
      "type": "n8n-nodes-base.postgres",
      "position": [
        3168,
        96
      ],
      "parameters": {
        "query": "-- =============================================================================\n-- 06_insert_ai_notification.sql\n-- -----------------------------------------------------------------------------\n-- Pegar en un nodo \"Postgres\" (Operation: \"Execute Query\") después de\n-- invocar al LLM y validar la respuesta.\n--\n-- Cubre los tres casos:\n--   a) Respuesta LLM válida → provider='openai'/'anthropic', is_fallback=false\n--   b) Respuesta LLM inválida → message_status='rejected', is_fallback=false\n--   c) Fallback a plantilla   → provider='template', is_fallback=true\n--\n-- El CHECK constraint en la tabla valida la coherencia provider↔is_fallback.\n-- =============================================================================\n\nINSERT INTO tfi.ai_notifications (\n    order_id,\n    provider, model, prompt_version,\n    prompt_tokens, completion_tokens, cost_usd, latency_ms,\n    message_text, message_status, is_fallback, delivery_channel,\n    error_message\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid,\n    $2, NULLIF($3::text, 'null'), NULLIF($4::text, 'null'),\n    NULLIF($5::text, 'null')::integer, NULLIF($6::text, 'null')::integer, NULLIF($7::text, 'null')::numeric, NULLIF($8::text, 'null')::integer,\n    $9, $10, NULLIF($11::text, 'null')::boolean, NULLIF($12::text, 'null'),\n    NULLIF($13::text, 'null')\n)\nRETURNING id AS notification_id, order_id, message_text, generated_at;\n-- order_id y message_text son necesarios para los nodos de despacho (07, 08).\n\n-- Parámetros:\n--   $1  = {{ $json.order_id }}\n--   $2  = 'openai' | 'anthropic' | 'template'\n--   $3  = 'gpt-4o-mini' | 'claude-haiku-...' | NULL si template\n--   $4  = 'v1' | 'v2' | ... | NULL si template\n--   $5  = {{ $json.usage.prompt_tokens || null }}\n--   $6  = {{ $json.usage.completion_tokens || null }}\n--   $7  = {{ $json.cost_usd || null }}\n--   $8  = {{ $json.latency_ms }}\n--   $9  = {{ $json.message_text }}\n--   $10 = 'generated' | 'validated' | 'rejected' | 'sent' | 'failed'\n--   $11 = true | false\n--   $12 = 'whatsapp' | 'email' | NULL\n--   $13 = {{ $json.error_message || null }}\n",
        "options": {
          "queryReplacement": "={{ $('Insert Order').item.json.order_id }}, {{ $json.provider }}, {{ $json.model }}, {{ $json.prompt_version }}, {{ $json.prompt_tokens || null }}, {{ $json.completion_tokens || null }}, {{ $json.cost_usd || 0 }}, {{ $json.latency_ms || 0 }}, {{ $json.message_text }}, {{ 'validated' }}, {{ $json.is_fallback || false }}, {{ 'whatsapp' }}, {{ null }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "1e27fadf-4dd6-41a3-bad9-98581276a65c",
      "name": "Merge dispatch ctx",
      "type": "n8n-nodes-base.code",
      "position": [
        3584,
        96
      ],
      "parameters": {
        "jsCode": " const aiNotif = $('Insert ai_notification').first().json;\n  const customerRow = $input.first().json;\n\n  const notificationId = aiNotif.notification_id || null;\n  const orderId        = aiNotif.order_id        || null;\n  const messageText    = aiNotif.message_text    || '';\n  const phone          = customerRow.phone       || null;\n  const fullName       = customerRow.full_name   || null;\n\n  return {\n      json: {\n          notification_id: notificationId,\n          order_id:        orderId,\n          message_text:    messageText,\n          phone,\n          full_name:       fullName,\n      },\n  };"
      },
      "typeVersion": 2
    },
    {
      "id": "2f35c017-5c83-46e9-a71a-6f40e3c82b07",
      "name": "Build WA payload",
      "type": "n8n-nodes-base.code",
      "position": [
        3792,
        96
      ],
      "parameters": {
        "jsCode": "// build-wa-payload.js\n// Arma el payload para el HTTP Request a la Cloud API de Meta.\n// Toma el teléfono del cliente (normalizado a E.164 argentino SIN \"9\").\n\nfunction toE164Argentina(raw) {\n    if (!raw) return null;\n    let digits = raw.replace(/\\D/g, '');\n    // Ya viene con \"9\" del prefijo mobile → sacamos el \"9\" para matchear\n    // el formato que registra Meta en su lista de recipients autorizados.\n    if (digits.startsWith('549') && digits.length >= 12) return '+54' + digits.slice(3);\n    if (digits.startsWith('54')  && digits.length >= 11) return '+' + digits;\n    if (digits.startsWith('0')) digits = digits.slice(1);\n    if (digits.length === 10) return '+54' + digits;\n    if (digits.length === 8)  return '+5411' + digits;\n    return null;\n}\n\nfunction buildGreeting(fullName) {\n    if (!fullName) return '';\n    const firstName = fullName.trim().split(/\\s+/)[0];\n    return 'Hola ' + firstName + '!\\n';\n}\n\nconst input = $json;\nconst customerRow = Array.isArray(input) ? input[0] : input;\n\nconst rawPhone       = customerRow.phone     || input.phone     || null;\nconst fullName       = customerRow.full_name || input.full_name || null;\nconst notificationId = input.notification_id || input.id        || null;\nconst messageText    = input.message_text    || input.message   || '';\nconst orderId        = input.order_id                           || null;\n\nconst phone = toE164Argentina(rawPhone);\n\nif (!phone) {\n    return {\n        json: {\n            no_phone: true,\n            reason: rawPhone ? 'phone_not_normalizable:' + rawPhone : 'phone_missing',\n            notification_id: notificationId,\n            order_id: orderId,\n        },\n    };\n}\n\nconst greeting     = buildGreeting(fullName);\nconst finalMessage = (greeting + messageText).trim();\n\nconst metaBody = {\n    messaging_product: 'whatsapp',\n    recipient_type:    'individual',\n    to:                phone,\n    type:              'text',\n    text: { preview_url: false, body: finalMessage },\n};\n\nreturn {\n    json: {\n        no_phone:        false,\n        notification_id: notificationId,\n        order_id:        orderId,\n        to_phone:        phone,\n        meta_body:       metaBody,\n        audit_event: {\n            event_type: 'dispatch_attempted',\n            severity:   'info',\n            component:  'dispatcher',\n            message:    'Intentando envío WhatsApp a ' + phone,\n            payload:    { notification_id: notificationId, to: phone },\n        },\n    },\n};"
      },
      "typeVersion": 2
    },
    {
      "id": "1e14dbf1-f78c-4481-ab2b-f4a844fb12cc",
      "name": "IF Phone ok?",
      "type": "n8n-nodes-base.if",
      "position": [
        4000,
        96
      ],
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 3,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "strict"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "ae9a5201-2d81-4359-ab27-c56fe8150c28",
              "operator": {
                "type": "boolean",
                "operation": "false",
                "singleValue": true
              },
              "leftValue": "={{ $json.no_phone }}",
              "rightValue": "false"
            }
          ]
        }
      },
      "typeVersion": 2.3
    },
    {
      "id": "bb62e2ed-abef-40ea-a9c7-70957663d4ea",
      "name": "Send Whatsapp",
      "type": "n8n-nodes-base.httpRequest",
      "position": [
        4208,
        0
      ],
      "parameters": {
        "url": "=https://graph.facebook.com/v20.0/{{ $env.WHATSAPP_PHONE_NUMBER_ID }}/messages ",
        "method": "POST",
        "options": {},
        "jsonBody": "={{ JSON.stringify($json.meta_body) }}   ",
        "sendBody": true,
        "specifyBody": "json",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth"
      },
      "credentials": {
        "httpBearerAuth": {
          "id": "xRSPdmDu8PnkEwWl",
          "name": "Bearer Auth account"
        },
        "httpHeaderAuth": {
          "id": "9mYf9C1iaP8glN5N",
          "name": "Header Auth account"
        }
      },
      "typeVersion": 4.4
    },
    {
      "id": "66b01077-f99c-4726-b1fd-cf86c1fe3e87",
      "name": "Parse WA response",
      "type": "n8n-nodes-base.code",
      "position": [
        4416,
        0
      ],
      "parameters": {
        "jsCode": " const input = $json;\n\n  const notificationId = input.notification_id || null;\n  const orderId        = input.order_id        || null;\n  const toPhone        = input.to_phone        || null;\n\n  const httpError = input.error || input.message || null;\n  const httpStatus = input.statusCode || input.status || null;\n  const isHttpError = !!httpError || (httpStatus && httpStatus >= 400);\n\n  if (isHttpError) {\n      const errorDetail = typeof httpError === 'object'\n          ? JSON.stringify(httpError)\n          : String(httpError || `HTTP ${httpStatus}`);\n\n      return {\n          json: {\n              notification_id: notificationId,\n              order_id:        orderId,\n              message_status:  'failed',\n              wa_message_id:   null,\n              error_reason:    errorDetail,\n              audit_event: {\n                  event_type: 'dispatch_failed',\n                  severity:   'error',\n                  component:  'dispatcher',\n                  message:    `Meta API rechazó el mensaje: ${errorDetail.slice(0, 200)}`,\n                  payload:    { notification_id: notificationId, to: toPhone, error: errorDetail },\n              },\n          },\n      };\n  }\n\n  const waMessageId = (input.messages && input.messages[0] && input.messages[0].id) || null;\n\n  if (!waMessageId) {\n      return {\n          json: {\n              notification_id: notificationId,\n              order_id:        orderId,\n              message_status:  'failed',\n              wa_message_id:   null,\n              error_reason:    'meta_response_missing_message_id',\n              audit_event: {\n                  event_type: 'dispatch_failed',\n                  severity:   'warning',\n                  component:  'dispatcher',\n                  message:    'Meta respondió 200 pero sin messages[0].id',\n                  payload:    { notification_id: notificationId, raw_response: input },\n              },\n          },\n      };\n  }\n\n  return {\n      json: {\n          notification_id: notificationId,\n          order_id:        orderId,\n          message_status:  'sent',\n          wa_message_id:   waMessageId,\n          error_reason:    null,\n          audit_event: {\n              event_type: 'dispatch_ok',\n              severity:   'info',\n              component:  'dispatcher',\n              message:    `Mensaje enviado por WhatsApp (wamid: ${waMessageId})`,\n              payload:    { notification_id: notificationId, to: toPhone, wa_message_id: waMessageId\n  },\n          },\n      },\n  };"
      },
      "typeVersion": 2
    },
    {
      "id": "b8343253-9b7d-4636-84ee-0da62cfb15dd",
      "name": "Update dispatch",
      "type": "n8n-nodes-base.postgres",
      "position": [
        4624,
        0
      ],
      "parameters": {
        "query": "UPDATE tfi.ai_notifications\nSET\n    message_status = $2,\n    sent_at        = CASE WHEN $2 = 'sent' THEN now() ELSE sent_at END,\n    wa_message_id  = NULLIF($3::text, 'null'),\n    error_message  = NULLIF($4::text, 'null')\nWHERE id = NULLIF($1::text, 'null')::uuid\nRETURNING id, message_status, sent_at, wa_message_id;",
        "options": {
          "queryReplacement": "{{ $('Insert ai_notification').item.json.notification_id }}, {{ $json.message_status || 'sent' }}, {{ $json.wa_message_id || null }}, {{ $json.error_reason || null }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "2af8b41f-b4b0-4552-9f42-b5077900d198",
      "name": "Audit dispatch",
      "type": "n8n-nodes-base.postgres",
      "position": [
        4816,
        0
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "={{ $('Insert Order').item.json.order_id }}, {{ 'message_sent' }}, {{ 'info' }}, {{ 'llm' }}, {{ 'Mensaje despachado por WhatsApp' }}, {{ JSON.stringify({delivery_channel: 'whatsapp', wamid: $json.wa_message_id || null}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "ab5041e7-3271-4b2e-8e9d-8ce5627e609e",
      "name": "Audit no_phone",
      "type": "n8n-nodes-base.postgres",
      "position": [
        4208,
        192
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "={{ $('Insert Order').item.json.order_id }}, {{ 'no_phone' }}, {{ 'warning' }}, {{ 'llm' }}, {{ 'Cliente sin telefono - no se envia WhatsApp' }}, {{ JSON.stringify({}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "d0879260-cc15-414c-83a1-57b778f88e91",
      "name": "Update dispatch (no phone)",
      "type": "n8n-nodes-base.postgres",
      "position": [
        4416,
        192
      ],
      "parameters": {
        "query": "UPDATE tfi.ai_notifications\nSET\n    message_status = $2,\n    sent_at        = CASE WHEN $2 = 'sent' THEN now() ELSE sent_at END,\n    wa_message_id  = NULLIF($3::text, 'null'),\n    error_message  = NULLIF($4::text, 'null')\nWHERE id = NULLIF($1::text, 'null')::uuid\nRETURNING id, message_status, sent_at, wa_message_id;",
        "options": {
          "queryReplacement": "={{ $('Insert ai_notification').item.json.notification_id }}, {{ 'failed' }}, {{ null }}, {{ 'no_phone' }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "5482649b-d10d-4340-8ef0-ae01eb9894eb",
      "name": "Audit dispatch_failed",
      "type": "n8n-nodes-base.postgres",
      "position": [
        4624,
        192
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "={{ $('Insert Order').item.json.order_id }}, {{ 'dispatch_failed' }}, {{ 'error' }}, {{ 'llm' }}, {{ 'Fallo al despachar mensaje' }}, {{ JSON.stringify({reason: 'send_failed'}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "63dd24a8-e0a4-4b88-acb3-a6088ea895d2",
      "name": "Webhook WC",
      "type": "n8n-nodes-base.webhook",
      "position": [
        480,
        -512
      ],
      "webhookId": "2bbd03eb-0c8d-4c68-aa4b-c2dd67df1d8f",
      "parameters": {
        "path": "wc-order",
        "options": {
          "rawBody": false
        },
        "httpMethod": "POST",
        "responseMode": "responseNode"
      },
      "typeVersion": 2.1
    },
    {
      "id": "fc36e376-270d-4414-828b-f1990aace8b6",
      "name": "Validate WC webhook",
      "type": "n8n-nodes-base.code",
      "position": [
        688,
        -512
      ],
      "parameters": {
        "jsCode": "// Loop over input items and add a new field called 'myNewField' to the const crypto = require('crypto');\n\nconst CHANNEL = 'woocommerce';\nconst SECRET = (typeof $env !== 'undefined' && $env.WC_WEBHOOK_SECRET) || '';\n\n// --- Helpers -----------------------------------------------------------------\n\n// Intenta recuperar el body CRUDO (bytes exactos) para validar la firma.\n// n8n con \"Raw Body: ON\" lo deja en item.binary.data (base64) o en rawBody.\nfunction getRawBody(item, bodyObj) {\n    if (item.json && typeof item.json.rawBody === 'string') return item.json.rawBody;\n    if (item.binary && item.binary.data && item.binary.data.data) {\n        try { return Buffer.from(item.binary.data.data, 'base64').toString('utf8'); }\n        catch (e) { /* ignore */ }\n    }\n    // Best-effort: re-serializa el objeto. Puede no coincidir byte a byte.\n    return { fallback: true, value: JSON.stringify(bodyObj) };\n}\n\n// Comparación en tiempo constante para evitar timing attacks.\nfunction safeEqual(a, b) {\n    const ba = Buffer.from(String(a));\n    const bb = Buffer.from(String(b));\n    if (ba.length !== bb.length) return false;\n    try { return crypto.timingSafeEqual(ba, bb); }\n    catch (e) { return false; }\n}\n\nfunction getHeader(headers, name) {\n    if (!headers) return null;\n    // n8n normaliza los headers a minúsculas, pero soportamos ambos por las dudas.\n    return headers[name] || headers[name.toLowerCase()] || null;\n}\n\n// --- Main loop ---------------------------------------------------------------\nconst results = [];\n\nfor (const item of $input.all()) {\n    const raw = item.json || {};\n    const headers = raw.headers || {};\n    // El Webhook node de n8n envuelve el payload en item.json.body.\n    const body = (raw.body && typeof raw.body === 'object') ? raw.body : raw;\n    const errors = [];\n    const appliedRules = [];\n\n    // 1. Detectar el ping de activación (WooCommerce manda {\"webhook_id\": N})\n    const isPing = body && body.webhook_id !== undefined\n        && body.id === undefined && body.line_items === undefined;\n    if (isPing) {\n        results.push({\n            json: {\n                valid: false, errors: ['ping'], is_ping: true, signature_valid: null,\n                order: null, channel: CHANNEL, applied_rules: ['info:webhook_ping'],\n                received_at: new Date().toISOString(),\n            },\n        });\n        continue;\n    }\n\n    // 2. Validar la firma HMAC-SHA256 (base64)\n    let signatureValid = null;\n    if (!SECRET) {\n        appliedRules.push('warn:wc_secret_not_configured_signature_skipped');\n    } else {\n        const provided = getHeader(headers, 'x-wc-webhook-signature');\n        if (!provided) {\n            errors.push('missing_signature_header');\n            signatureValid = false;\n        } else {\n            const rawBody = getRawBody(item, body);\n            const rawValue = (rawBody && rawBody.fallback) ? rawBody.value : rawBody;\n            if (rawBody && rawBody.fallback) {\n                appliedRules.push('warn:raw_body_unavailable_used_stringify');\n            }\n            const expected = crypto.createHmac('sha256', SECRET)\n                .update(rawValue, 'utf8').digest('base64');\n            signatureValid = safeEqual(expected, provided);\n            if (!signatureValid) errors.push('invalid_signature');\n        }\n    }\n\n    // 3. Estructura mínima del pedido\n    if (body.id === undefined || body.id === null) errors.push('missing_field:id');\n    if (!Array.isArray(body.line_items)) errors.push('missing_field:line_items');\n    if (!body.billing || typeof body.billing !== 'object') {\n        appliedRules.push('warn:billing_missing');\n    }\n\n    results.push({\n        json: {\n            valid: errors.length === 0,\n            errors,\n            is_ping: false,\n            signature_valid: signatureValid,\n            order: body,\n            channel: CHANNEL,\n            applied_rules: appliedRules,\n            received_at: new Date().toISOString(),\n        },\n    });\n}\n\nreturn results;;"
      },
      "typeVersion": 2
    },
    {
      "id": "c1a1c4bc-c73d-4abe-9df4-b6131a13d7f3",
      "name": "Valid",
      "type": "n8n-nodes-base.if",
      "position": [
        896,
        -512
      ],
      "parameters": {
        "options": {},
        "conditions": {
          "options": {
            "version": 3,
            "leftValue": "",
            "caseSensitive": true,
            "typeValidation": "strict"
          },
          "combinator": "and",
          "conditions": [
            {
              "id": "53c9401c-a17e-41c1-8e41-07c3e1e7bf41",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              },
              "leftValue": "={{ $json.valid }}",
              "rightValue": ""
            }
          ]
        }
      },
      "typeVersion": 2.3
    },
    {
      "id": "0923b960-9db8-4fc8-aa8d-fcdfb00c3152",
      "name": "Normalize WC order",
      "type": "n8n-nodes-base.code",
      "position": [
        928,
        -320
      ],
      "parameters": {
        "jsCode": "// =============================================================================\n// normalize-wc-order.js\n// -----------------------------------------------------------------------------\n// Pegar en un nodo \"Code\" de n8n (modo: \"Run Once for All Items\") ubicado\n// inmediatamente después del Webhook trigger de WooCommerce.\n//\n// Diferencia respecto del flujo ML:\n//   - WC manda el pedido COMPLETO en el webhook (no necesita enrich extra)\n//   - El shape es muy distinto: billing/shipping objects, line_items array,\n//     status en lowercase con prefijos como 'wc-' en algunos casos\n//\n// Esta función traduce ese shape al MODELO CANÓNICO compartido con ML:\n//   { customer, order, items, meta }\n//\n// A partir de este punto, el resto del pipeline es idéntico al de ML\n// (idempotency → persist → IA → WhatsApp), por eso converge en el mismo\n// formato canónico.\n// =============================================================================\n\nconst crypto = require('crypto');\n\nconst NORMALIZATION_VERSION = 'v1';\nconst CHANNEL = 'woocommerce';\n\n// --- Mapeo de estados WC → modelo canónico ----------------------------------\n// WC nativos: pending, processing, on-hold, completed, cancelled, refunded, failed\nconst STATUS_MAP = {\n    'pending':     'pending_payment',\n    'on-hold':     'pending_payment',\n    'processing':  'paid',         // pago confirmado, en preparación\n    'completed':   'delivered',    // WC marca completed cuando ya se envió/entregó\n    'cancelled':   'cancelled',\n    'refunded':    'refunded',\n    'failed':      'error',\n};\n\n// --- Helpers (mismas funciones que el normalizador de ML para consistencia) -\nfunction sha256(value) {\n    if (value === null || value === undefined || value === '') return null;\n    return crypto.createHash('sha256').update(String(value).toLowerCase().trim()).digest('hex');\n}\nfunction makePseudonym(channel, externalId) {\n    const hash = crypto.createHash('sha256').update(`${channel}:${externalId}`).digest('hex').slice(0, 12);\n    return `cust_${hash}`;\n}\n\n// --- Main loop --------------------------------------------------------------\nconst out = [];\n\nfor (const item of $input.all()) {\n    const t0 = Date.now();\n    // v1.3: entre Validate WC webhook y este nodo se insertaron Persist raw\n    // event WC + ACK 200 WC, por lo que $input ya no trae el body del pedido.\n    // Se lee el pedido directamente desde Validate WC webhook, que lo emite\n    // como `.order` (ver validate-wc-webhook.js).\n    const wc = $('Validate WC webhook').item.json.order || {};\n\n    const appliedRules = [];\n    const billing = wc.billing || {};\n    const shipping = wc.shipping || {};\n\n    // El nombre real puede estar en billing (típicamente) o en shipping\n    const fullName = [billing.first_name, billing.last_name].filter(Boolean).join(' ').trim()\n        || [shipping.first_name, shipping.last_name].filter(Boolean).join(' ').trim()\n        || null;\n    const phone = billing.phone || shipping.phone || null;\n    const email = billing.email || null;\n\n    // WC tiene dos identificadores posibles del cliente:\n    //   - customer_id (numérico, si es usuario registrado; 0 para guest checkouts)\n    //   - email (fallback estable cuando es guest)\n    let externalCustomerId = wc.customer_id && wc.customer_id !== 0\n        ? String(wc.customer_id)\n        : (email || null);\n\n    // D1 (v1.3): si ninguno de los dos está disponible, se genera un identificador\n    // sustituto determinístico `synthetic:{channel}:{order.external_id}`. Es\n    // reproducible (mismo pedido reingresado hace UPSERT sobre la misma fila) y\n    // trazable (el prefijo permite filtrar y contar sustituciones). Preserva la\n    // integridad referencial (customers.external_id NOT NULL) sin bloquear el\n    // pedido, según §4.6 del TFI corregido.\n    if (!externalCustomerId) {\n        externalCustomerId = `synthetic:${CHANNEL}:${wc.id}`;\n        appliedRules.push('warn:customer_id_missing_substituted');\n    }\n\n    const customer = {\n        external_id: externalCustomerId,\n        channel: CHANNEL,\n        pseudonym: makePseudonym(CHANNEL, externalCustomerId),\n        full_name: fullName,\n        email,\n        phone,\n        email_hash: sha256(email),\n        phone_hash: sha256(phone),\n    };\n\n    // Status: WC a veces prefija con 'wc-' (status interno). Lo limpiamos.\n    const rawStatus = String(wc.status || '').replace(/^wc-/, '').toLowerCase();\n    const status = STATUS_MAP[rawStatus] || 'error';\n    if (status === 'error') appliedRules.push(`warn:unmapped_status:${rawStatus}`);\n\n    const order = {\n        external_id: String(wc.id),\n        channel: CHANNEL,\n        status,\n        total_amount: Number(wc.total) || 0,\n        currency: wc.currency || 'ARS',\n        source_created_at: wc.date_created_gmt || wc.date_created || new Date().toISOString(),\n        raw_payload: wc,\n        normalization_version: NORMALIZATION_VERSION,\n    };\n\n    // Estado de envío: WC no tiene un campo shipping.status como ML. Lo derivamos\n    // del estado del pedido.\n    const deliveryStatus =\n        status === 'cancelled' ? 'cancelled' :\n        status === 'delivered' ? 'delivered' :\n        status === 'paid'      ? 'preparing' :\n                                 'pending';\n\n    const items = (wc.line_items || []).map((li) => ({\n        sku: li.sku || String(li.product_id) || null,\n        product_name: li.name || '(sin título)',\n        quantity: Number(li.quantity) || 1,\n        // line_items.price es el unitario; total/subtotal son los del item completo\n        unit_price: Number(li.price ?? li.subtotal) || 0,\n        delivery_status: deliveryStatus,\n        metadata: {\n            wc_item_id: li.id ?? null,\n            wc_product_id: li.product_id ?? null,\n            wc_variation_id: li.variation_id ?? null,\n            tax_total: li.total_tax ?? null,\n        },\n    }));\n    if (items.length === 0) appliedRules.push('warn:no_items');\n\n    // v1.3: raw_event_id y ack_at vienen del nodo Persist raw event WC\n    // (§4.1.1 del TFI corregido). El ack_at se persiste en tfi.orders y la\n    // ventana received_at→ack_at debe quedar por debajo de 500 ms.\n    const rawEvt = $('Persist raw event WC').item.json;\n\n    out.push({\n        json: {\n            customer,\n            order,\n            items,\n            raw_event_id: rawEvt.raw_event_id,\n            ack_at: rawEvt.ack_at,\n            meta: {\n                normalization_ms: Date.now() - t0,\n                applied_rules: appliedRules,\n                normalization_version: NORMALIZATION_VERSION,\n                source_channel: CHANNEL,\n            },\n        },\n    });\n}\n\nreturn out;\n"
      },
      "typeVersion": 2
    },
    {
      "id": "4da1edf6-fd2a-4de3-a044-38319ee74ead",
      "name": "Audit",
      "type": "n8n-nodes-base.postgres",
      "position": [
        1104,
        -416
      ],
      "parameters": {
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nVALUES (\n    NULLIF($1::text, 'null')::uuid, $2, $3, $4,\n    NULLIF($5::text, 'null'), $6::jsonb,\n    NULLIF($7::text, 'null')::integer,\n    COALESCE(NULLIF($8::text, 'null')::integer, 0)\n)\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "=null, {{ 'validation_failed' }}, {{ 'warning' }}, {{ 'webhook' }}, {{ 'Webhook WC invalido' }}, {{ JSON.stringify({errors: $('Validate WC webhook').item.json.errors}) }}, {{ null }}, {{ 0 }}"
        },
        "operation": "executeQuery"
      },
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.6
    },
    {
      "id": "14dc7605-8884-4337-97d0-3ebe936a3939",
      "name": "Respond ",
      "type": "n8n-nodes-base.respondToWebhook",
      "position": [
        1312,
        -416
      ],
      "parameters": {
        "options": {
          "responseCode": 200
        }
      },
      "typeVersion": 1.5
    },
    {
      "id": "66bca19f-28f7-447d-9431-6f43afd409ab",
      "name": "Route to canonical",
      "type": "n8n-nodes-base.code",
      "position": [
        1032,
        -96
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return $input.all();",
        "language": "javaScript"
      },
      "typeVersion": 2
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO tfi.raw_events (channel, payload)\nVALUES ($1, $2::jsonb)\nRETURNING id AS raw_event_id, received_at AS ack_at;",
        "options": {
          "queryReplacement": "={{ \"mercadolibre\" }}, {{ JSON.stringify($(\"Validate webhook\").item.json.webhook) }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        520,
        -220
      ],
      "id": "v13-001",
      "name": "Persist raw event ML",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "text",
        "responseBody": "OK",
        "options": {
          "responseCode": 200
        }
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        520,
        -100
      ],
      "id": "v13-002",
      "name": "ACK 200 ML"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO tfi.raw_events (channel, payload)\nVALUES ($1, $2::jsonb)\nRETURNING id AS raw_event_id, received_at AS ack_at;",
        "options": {
          "queryReplacement": "={{ \"woocommerce\" }}, {{ JSON.stringify($(\"Validate WC webhook\").item.json.order) }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        896,
        -650
      ],
      "id": "v13-003",
      "name": "Persist raw event WC",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "text",
        "responseBody": "OK",
        "options": {
          "responseCode": 200
        }
      },
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        1000,
        -650
      ],
      "id": "v13-004",
      "name": "ACK 200 WC"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE tfi.raw_events\n   SET processed_at = now()\n WHERE id = $1\nRETURNING id, received_at, processed_at,\n          EXTRACT(EPOCH FROM (processed_at - received_at)) * 1000 AS processing_ms;",
        "options": {
          "queryReplacement": "={{ $(\"Route to canonical\").item.json.raw_event_id }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        1616,
        -192
      ],
      "id": "v13-005",
      "name": "Complete raw event (dupe)",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE tfi.raw_events\n   SET processed_at = now()\n WHERE id = $1\nRETURNING id, received_at, processed_at,\n          EXTRACT(EPOCH FROM (processed_at - received_at)) * 1000 AS processing_ms;",
        "options": {
          "queryReplacement": "={{ $(\"Route to canonical\").item.json.raw_event_id }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        5040,
        0
      ],
      "id": "v13-006",
      "name": "Complete raw event (ok)",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "UPDATE tfi.raw_events\n   SET processed_at = now()\n WHERE id = $1\nRETURNING id, received_at, processed_at,\n          EXTRACT(EPOCH FROM (processed_at - received_at)) * 1000 AS processing_ms;",
        "options": {
          "queryReplacement": "={{ $(\"Route to canonical\").item.json.raw_event_id }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        4816,
        192
      ],
      "id": "v13-007",
      "name": "Complete raw event (dispatch_failed)",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "INSERT INTO tfi.audit_log (\n    order_id, event_type, severity, component,\n    message, payload, duration_ms, retry_attempt\n)\nSELECT\n    NULL, 'normalization_warnings', 'warning', 'normalizer',\n    'Reglas aplicadas por el normalizador (buyer_id sustituto, estados no mapeados, etc.)',\n    $1::jsonb, NULL, 0\nWHERE jsonb_array_length(\n        COALESCE(($1::jsonb)->'applied_rules', '[]'::jsonb)\n      ) > 0\nRETURNING id AS audit_id, created_at;",
        "options": {
          "queryReplacement": "={{ JSON.stringify({channel: $json.order.channel, external_id: $json.order.external_id, applied_rules: $json.meta.applied_rules}) }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.6,
      "position": [
        1036,
        -240
      ],
      "id": "v13-100",
      "name": "Audit normalization warnings",
      "credentials": {
        "postgres": {
          "id": "vNyNbhNBYftjmCca",
          "name": "Postgres account"
        }
      }
    }
  ],
  "connections": {
    "Audit": {
      "main": [
        [
          {
            "node": "Respond ",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Valid": {
      "main": [
        [
          {
            "node": "Persist raw event WC",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Audit",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Valid?": {
      "main": [
        [
          {
            "node": "Persist raw event ML",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Audit validation_failed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook WC": {
      "main": [
        [
          {
            "node": "Validate WC webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Call OpenAI": {
      "main": [
        [
          {
            "node": "Parse LLM Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Split Items": {
      "main": [
        [
          {
            "node": "Insert Items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF Phone ok?": {
      "main": [
        [
          {
            "node": "Send Whatsapp",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Audit no_phone",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Insert Items": {
      "main": [
        []
      ]
    },
    "Insert Order": {
      "main": [
        [
          {
            "node": "Split Items",
            "type": "main",
            "index": 0
          },
          {
            "node": "Build LLM prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "If Duplicate?": {
      "main": [
        [
          {
            "node": "Audit duplicate",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Upsert customer",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Send Whatsapp": {
      "main": [
        [
          {
            "node": "Parse WA response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Use Fallback?": {
      "main": [
        [
          {
            "node": "Fallback template",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Insert ai_notification",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit dispatch": {
      "main": [
        [
          {
            "node": "Complete raw event (ok)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit no_phone": {
      "main": [
        [
          {
            "node": "Update dispatch (no phone)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit duplicate": {
      "main": [
        [
          {
            "node": "Complete raw event (dupe)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Update dispatch": {
      "main": [
        [
          {
            "node": "Audit dispatch",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upsert customer": {
      "main": [
        [
          {
            "node": "Insert Order",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build LLM prompt": {
      "main": [
        [
          {
            "node": "Call OpenAI",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build WA payload": {
      "main": [
        [
          {
            "node": "IF Phone ok?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Enrich from mock": {
      "main": [
        [
          {
            "node": "Normalize ML order",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate webhook": {
      "main": [
        [
          {
            "node": "Valid?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook ML order": {
      "main": [
        [
          {
            "node": "Validate webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Check idempotency": {
      "main": [
        [
          {
            "node": "If Duplicate?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fallback template": {
      "main": [
        [
          {
            "node": "Insert ai_notification",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse WA response": {
      "main": [
        [
          {
            "node": "Update dispatch",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get customer phone": {
      "main": [
        [
          {
            "node": "Merge dispatch ctx",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Merge dispatch ctx": {
      "main": [
        [
          {
            "node": "Build WA payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize ML order": {
      "main": [
        [
          {
            "node": "Route to canonical",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize WC order": {
      "main": [
        [
          {
            "node": "Route to canonical",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse LLM Response": {
      "main": [
        [
          {
            "node": "Use Fallback?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Route to canonical": {
      "main": [
        [
          {
            "node": "Audit normalization warnings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate WC webhook": {
      "main": [
        [
          {
            "node": "Valid",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Insert ai_notification": {
      "main": [
        [
          {
            "node": "Get customer phone",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit validation_failed": {
      "main": [
        [
          {
            "node": "Respond 400",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Update dispatch (no phone)": {
      "main": [
        [
          {
            "node": "Audit dispatch_failed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Persist raw event ML": {
      "main": [
        [
          {
            "node": "ACK 200 ML",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "ACK 200 ML": {
      "main": [
        [
          {
            "node": "Enrich from mock",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Persist raw event WC": {
      "main": [
        [
          {
            "node": "ACK 200 WC",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "ACK 200 WC": {
      "main": [
        [
          {
            "node": "Normalize WC order",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit dispatch_failed": {
      "main": [
        [
          {
            "node": "Complete raw event (dispatch_failed)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Audit normalization warnings": {
      "main": [
        [
          {
            "node": "Check idempotency",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  },
  "pinData": {},
  "meta": {
    "templateCredsSetupCompleted": true
  }
}